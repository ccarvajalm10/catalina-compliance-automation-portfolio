# Optional. Without it, the app runs in DEMO MODE using pre-computed analysis fixtures.
# With it set, the /tool page and `npm run eval` can call the live model.
ANTHROPIC_API_KEY=

# Optional overrides
ANTHROPIC_MODEL=claude-sonnet-5
